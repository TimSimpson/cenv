[project]
name = "cenv"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools]
packages = { find = { where = ["python"]}}

[dependency-groups]
dev = [
    "coverage>=7.7.1",
    "maturin>=1.8.3",
    "mypy>=1.15.0",
    "pytest>=8.3.5",
]

